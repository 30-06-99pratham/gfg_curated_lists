// here the main key is to run loop from last element 
// and do a[i] = a[i-1], since by ding this the a[i-1] will not be changed.

// algo
  // int temp = a[n-1]
  // for(int i = n-1;i>0;i--){
  //  a[i] = a[i-1]
  //   }
  // a[0] = temp;
